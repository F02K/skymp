{
  "name": "@skymp/managed-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run check:api-contract && npm run build && node --test test/*.test.mjs",
    "check:api-contract": "node ../scripts/check-api-contract.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.2.2"
  },
  "optionalDependencies": { "pg": "^8.13.1" }
}
